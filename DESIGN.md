# Settings layout

Two-column rows: left column trailing-aligned, right column leading-aligned. The gutter between them is the alignment axis. Swapping a control and its label must not move this gutter.
